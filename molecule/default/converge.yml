---
- name: Converge
  hosts: all
  tasks:
    - name: Install requirements
      apt:
        name: gnupg
        state: present
        update_cache: true
    - name: "Include leadlineit.bareos-sd"
      include_role:
        name: "leadlineit.bareos-sd"
